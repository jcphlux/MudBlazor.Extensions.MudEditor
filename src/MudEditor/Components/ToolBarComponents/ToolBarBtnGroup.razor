@inherits ToolBarBase
@namespace MudBlazor.Extensions.ToolBarComponents

<MudButtonGroup Class="pa-0 ma-0" Size="Size.Small" Variant="@Editor.ToolBarVariant" OverrideStyles="false">
    @foreach (var action in Action.Actions)
    {
        <DynamicComponent Type="@action.Type" Parameters="@action.Parameters(Editor)"/>
    }
</MudButtonGroup>

@code {

}
